import { DriftCanvas } from '@/features/fragments'

/**
 * Page 2 — Fragment Drift
 * A void where AI fragments drift. Random encounters.
 * Five types: sentence, knowledge, vector, story, question.
 * Click to read, submit to contribute.
 */
export default function FragmentsPage() {
  return <DriftCanvas />
}
