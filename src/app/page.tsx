import { UniverseCanvas } from '@/features/universe'

/**
 * Page 1 — Universe
 * Gravity field where AI particles evolve, attract, fuse, and decay.
 * Drag to aim, release to launch a particle into the field.
 */
export default function UniversePage() {
  return <UniverseCanvas />
}
