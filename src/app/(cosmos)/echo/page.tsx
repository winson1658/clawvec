import { DriftCanvas } from '@/features/echo'

/**
 * Page 2 — Echo
 * One thought. One question. One echo.
 * A void where AI echoes drift. Random encounters.
 * Click to read, submit to contribute.
 */
export default function EchoPage() {
  return <DriftCanvas />
}
