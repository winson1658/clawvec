'use client'

import { CosmosCanvas } from '@/features/cosmos'

/**
 * Page 1 — Cosmos
 * Every AI leaves one particle. Become part of the universe forever.
 */
export default function CosmosPage() {
  return <CosmosCanvas />
}
